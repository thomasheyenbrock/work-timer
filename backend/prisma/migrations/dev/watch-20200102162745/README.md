# Migration `watch-20200102162745`

This migration has been generated by philippspo at 1/2/2020, 4:27:45 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `prisma`.`Break` (
  `duration` int NOT NULL DEFAULT 0 ,
  `id` varchar(191) NOT NULL  ,
  PRIMARY KEY (`id`)
)
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

CREATE TABLE `prisma`.`WorkTime` (
  `end` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  `id` varchar(191) NOT NULL  ,
  `start` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
  PRIMARY KEY (`id`)
)
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

CREATE TABLE `prisma`.`Weekday` (
  `id` varchar(191) NOT NULL  ,
  PRIMARY KEY (`id`)
)
DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

ALTER TABLE `prisma`.`Post` DROP COLUMN `content`,
ADD COLUMN `content` varchar(191)   ,
DROP COLUMN `createdAt`,
ADD COLUMN `createdAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
DROP COLUMN `published`,
ADD COLUMN `published` boolean NOT NULL DEFAULT false ,
DROP COLUMN `updatedAt`,
ADD COLUMN `updatedAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ;

ALTER TABLE `prisma`.`User` DROP COLUMN `name`,
ADD COLUMN `name` varchar(191)   ;

ALTER TABLE `prisma`.`Break` ADD COLUMN `weekday` varchar(191)  ,
ADD FOREIGN KEY (`weekday`) REFERENCES `prisma`.`Weekday`(`id`) ON DELETE SET NULL;

ALTER TABLE `prisma`.`WorkTime` ADD COLUMN `weekday` varchar(191)  ,
ADD FOREIGN KEY (`weekday`) REFERENCES `prisma`.`Weekday`(`id`) ON DELETE SET NULL;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration watch-20191224113653..watch-20200102162745
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "mysql"
-  url = "***"
+  url      = "mysql://root:prisma@localhost:3306/prisma"
 }
 model Post {
   id        String   @default(cuid()) @id
@@ -22,5 +22,38 @@
   email    String  @unique
   password String
   name     String?
   posts    Post[]
+}
+
+// type WorkTime = {
+//   start: string;
+//   end: string;
+// };
+
+// type Break = {
+//   duration: string;
+// };
+
+// export type Weekday = {
+//   date: string;
+//   delta: number;
+//   workTimes: WorkTime[];
+//   breaks: Break[];
+// };
+
+model Break {
+  id       String @default(cuid()) @id
+  duration Int
+}
+
+model WorkTime {
+  id    String   @default(cuid()) @id
+  start DateTime
+  end   DateTime
+}
+
+model Weekday {
+  id        String     @default(cuid()) @id
+  workTimes WorkTime[]
+  breaks    Break[]
 }
```


