# Migration `watch-20200103121716`

This migration has been generated by philippspo at 1/3/2020, 12:17:18 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `prisma`.`Post` DROP COLUMN `content`,
ADD COLUMN `content` varchar(191)   ,
DROP COLUMN `createdAt`,
ADD COLUMN `createdAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
DROP COLUMN `published`,
ADD COLUMN `published` boolean NOT NULL DEFAULT false ,
DROP COLUMN `updatedAt`,
ADD COLUMN `updatedAt` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ;

ALTER TABLE `prisma`.`User` DROP COLUMN `name`,
ADD COLUMN `name` varchar(191)   ;

ALTER TABLE `prisma`.`Weekday` ADD COLUMN `date` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ;

ALTER TABLE `prisma`.`WorkTime` DROP COLUMN `end`,
ADD COLUMN `end` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ,
DROP COLUMN `start`,
ADD COLUMN `start` datetime(3) NOT NULL DEFAULT '1970-01-01 00:00:00' ;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration watch-20200102162745..watch-20200103121716
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "mysql"
-  url = "***"
+  url      = "mysql://root:prisma@localhost:3306/prisma"
 }
 model Post {
   id        String   @default(cuid()) @id
@@ -24,24 +24,8 @@
   name     String?
   posts    Post[]
 }
-// type WorkTime = {
-//   start: string;
-//   end: string;
-// };
-
-// type Break = {
-//   duration: string;
-// };
-
-// export type Weekday = {
-//   date: string;
-//   delta: number;
-//   workTimes: WorkTime[];
-//   breaks: Break[];
-// };
-
 model Break {
   id       String @default(cuid()) @id
   duration Int
 }
@@ -53,7 +37,8 @@
 }
 model Weekday {
   id        String     @default(cuid()) @id
+  date      DateTime
   workTimes WorkTime[]
   breaks    Break[]
 }
```


